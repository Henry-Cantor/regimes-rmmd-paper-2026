# STRONG-RMMD — Python dependencies.
# Tested with the versions in the right-hand comments (Python 3.11); the lower bounds are
# conservative. For GPU training install a CUDA-enabled torch build (see https://pytorch.org).
torch>=2.0          # tested 2.7.1   — the model, training, autograd
numpy>=1.24         # tested 2.1.3
scipy>=1.10         # tested 1.15.3  — stats for the SUT/theorem tests (ANCOVA, permutation)
matplotlib>=3.6     # tested 3.10.0  — paper figures
netCDF4>=1.6        # tested 1.7.4   — reading TRANSP CDF outputs (phase0 data pipeline)

# Only needed to execute the paper-figures notebook (CPU is fine):
nbformat>=5.7       # tested 5.10.4
nbconvert>=7.0      # tested 7.16.6
jupyter>=1.0
